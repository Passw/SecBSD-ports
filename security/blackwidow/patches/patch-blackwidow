$OpenBSD$

Index: blackwidow
--- blackwidow.orig
+++ blackwidow
@@ -274,7 +274,7 @@ else:
     else:
       port = port
 
-  save_dir = "/usr/share/blackwidow/" + domain + "_" + port + "/"
+  save_dir = "~/blackwidow/" + domain + "_" + port + "/"
   os.system('mkdir -p ' + save_dir + ' 2>/dev/null')
   atexit.register(exit_handler)
 
